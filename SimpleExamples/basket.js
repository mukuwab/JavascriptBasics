
    function count(...basket) {
        //creates a new array based on input --> spread operator

        //print the length of the new array
        console.log(basket.length)
            
        console.log(`\nThe numbers in the basket are ${basket}.`)
    }

    count(10, 9, 8, 7, 6);



